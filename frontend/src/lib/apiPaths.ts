export const API_PATHS = {
  common: {
    health: "/api/common/health/",
  },
  bulkRegister: {
    inputDefs: "/api/common/input-defs/",
    register: "/api/common/bulk-register/",
  },
} as const;
